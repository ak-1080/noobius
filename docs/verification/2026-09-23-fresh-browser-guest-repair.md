# Fresh-browser hosted guest repair — September 23, 2026

Tested the deployed `https://play.noobius.io/` in a Chrome profile with no prior Noobius guest save. The separate existing in-app-browser guest save was not reset or used for this test.

- Entered as a guest, named the character `QA Shift`, kept the default outfit, advanced through all five tutorial slides and entered the 3D center.
- Followed the first highlighted objective to build the free starter machine. The center showed 24 Compute/min production and a new backup-machine option.
- Chose a repair offer from the job board, accepted it, followed the guided route to the scrap pile, and pressed `E` at the pile. The required three scrap were available afterward.
- Followed the route to the worksite, started the repair, inspected three readings (`Network: connected`, `Power: normal`, `Temperature: too hot`), chose to flush the cooling line, waited for the system to settle, tested it, and collected 38 Compute, 15 reputation and one job report. A new repair offer appeared.
- Reloaded the hosted page, chose **Continue guest game**, and verified that the floor still showed 38 Compute and the machine had stored idle earnings. The guest save remained scoped to that browser profile.
- Visually inspected the 3D center, character, machine labels and HUD at the browser's desktop viewport. No obvious clipping was visible.
- Independently reloaded an older in-app browser tab before checking the wallet picker. Its pre-reload JavaScript still showed the obsolete Ethereum/EVM tab; the refreshed hosted release showed only Phantom, Solflare and Backpack. This was a stale open tab, not a current deployed UI defect.

This is one rendered desktop-browser guest path. It does not verify a physical phone, a real Solana wallet extension, signed-account save, browser-level multiplayer, token payment or all later realms.
