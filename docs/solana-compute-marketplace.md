# Compute marketplace: Solana settlement protocol

This records the implemented server protocol and remaining launch checks. Token checkout is not live. Players buy each other's earned Compute with a chosen SPL mint. No guaranteed redemption, new Compute token, deposited wallet balance or custom DEX is required.

## Hosting boundary

Cloudflare Workers, D1 and the room coordinator host the game and its marketplace ledger. They do not operate the Solana network for this project. [Cloudflare's Web3 gateway documentation](https://developers.cloudflare.com/web3/) currently lists IPFS and Ethereum gateways, not a Solana RPC gateway. The game and recovery Worker can call a configured HTTPS Solana RPC endpoint, but [Solana's production guidance](https://solana.com/docs/tools/production-readiness) says its public mainnet endpoint is rate-limited with no SLA and should not be used for production payments. A dedicated private Solana RPC provider, ideally with a separately tested history provider, is therefore a separate live-trading dependency; upgrading Cloudflare Workers does not supply it. Keep any provider credential in a Cloudflare secret rather than a committed config or browser bundle. An optional `NOOBIUS_TOKEN_RPC_FALLBACK_URL` lets checkout and recovery retry an unavailable primary and look up a transaction missing from the primary's history. The fallback's genesis is checked before use; two null results still cannot prove non-execution. Configure the exact same primary and fallback URLs on the game and recovery Workers, and rehearse an actual historical-transaction lookup before live trading.

## The reservation problem

A simple expiring listing plus an unsigned direct-transfer transaction is insufficient: a buyer could broadcast after the app thinks checkout was abandoned. Releasing the seller's Compute on a timer could leave a paid buyer without goods. The backend must know the exact possible transaction before it becomes broadcastable.

Use a standard SPL transfer transaction with an additional signed Memo instruction. The Memo requires the buyer and a dedicated application authorization signer. The buyer signs first. The server checks that the transaction message exactly matches its durable quote, validates the buyer's signature, durably saves the buyer transaction/signature and submission intent, then adds the application signature. Only after persistence may the complete transaction leave the server. A buyer cannot independently broadcast the original incomplete quote without the application signature. This signer holds no player tokens and grants no spending authority: the buyer still signs the exact transfer amounts, asset, destinations, blockhash and memo. Compromise still permits unwanted authorization, so store/rotate it as a production secret and never expose it to client code.

References: [Solana Memo signer checks](https://www.solana-program.com/docs/memo), [transaction signatures and atomic instructions](https://solana.com/docs/core/transactions).

## Required state transitions

- Seller lists a bounded integer amount of earned, server-authoritative Compute at a token-denominated price. Atomically debit available Compute into a dedicated reservation ledger with an immutable listing ID and request receipt. Guest/local saves cannot enter this ledger.
- One buyer reserves an open listing. Bind exact buyer/seller wallet, mint, network genesis, decimals, amount, fee split, authorization signer, recent blockhash, last-valid block height and exact serialized message to a durable quote. Repeated calls return the same quote. Self-trades, wrong ecosystem and simultaneous buyers are rejected.
- Buyer reviews the quote and signs through `solana:signTransaction`. A login signature is never a payment approval. Unsupported signing capabilities get an explicit error, not a fallback that changes settlement semantics.
- On signed submission, verify byte-for-byte message identity, all required buyer signatures, zero unexpected signers/instructions and the immutable quote identity. Persist the exact buyer signature/submission intent **before** adding the app signature or broadcasting.
- Retry only the identical serialized transaction. Never refresh its blockhash or issue a new payment while a prior signature may settle. Different signatures or messages for a reserved quote are conflicts.
- On trusted RPC finalized success, verify the exact on-chain transaction, mint/amount/participants and recorded signature. Atomically consume the seller reservation, credit the buyer exactly once and write a settlement receipt. Concurrent retries must converge.
- An unsubmitted quote may expire safely only if no server authorization was issued. Submitted/uncertain payments retain the reservation. On a finalized failure, release through a deduplicated recovery transition. A buyer-only signature that was never co-signed or exposed by the server can also expire after its blockhash is invalid at a finalized root. Once a complete transaction could have been broadcast, a missing RPC transaction/status is ambiguous and does not release the reservation automatically.
- Reconcile pending submissions independently of the buyer's browser. Before any database restore, freeze settlement and reconcile against Solana; database time travel does not unwind token transfers.

## Initial bounds

The configured mint can use the original SPL Token Program or Token-2022. Set `NOOBIUS_TOKEN_PROGRAM` to the exact owner program address of `NOOBIUS_TOKEN_MINT`; the original program remains the default for legacy configuration. This setting is part of the listing/realm policy identity and the immutable payment quote. Token-2022 checkout accepts ordinary mints and metadata-only extensions. It rejects transfer fees, hooks, and any other behavior-changing or unknown mint extension so the seller receives exactly the quoted amount. A StonkFun launch mint must be checked on-chain before enabling trading. Quote fixed token amounts rather than an unverified USD oracle. No platform fee until a real treasury destination and fee policy are configured. Buyer pays visible SOL network/rent costs. Limit open listings, pending quotes per wallet, amount per trade and submission retries.

After the real mint exists, run `npm run preflight:solana-mint` with `NOOBIUS_TOKEN_MINT`, `NOOBIUS_SOLANA_NETWORK=mainnet-beta`, and a trusted HTTPS `NOOBIUS_TOKEN_RPC_URL` set in the environment. If a second provider is configured, also set `NOOBIUS_TOKEN_RPC_FALLBACK_URL`; preflight checks both providers against the same chain, mint, extensions and finalized blockhash requirements. This read-only check reports the on-chain token program, decimals, extensions and finalized slot. A passing report does not prove archival history, browser signing or paid settlement; do not enable trading until those pass on staging and the real asset's rules are reviewed.

## Acceptance evidence still needed

Two wallet buyers racing for one listing; insufficient balance; wrong mint/network/recipient/decimals; altered memo/amount/instructions; invalid or replayed signatures; quote expiry before signing; crash before/after saving submission intent; ambiguous broadcast result; retry after successful payment; receipt delivery failure; cancellation racing checkout; RPC unavailable; frontend reload during pending payment; finalized failed payment; recovery job retries; old configuration/key rotation; restore reconciliation. Exercise actual test-token transactions and real wallet UI before enabling mainnet.

## Implementation checkpoint — September 22, 2026

Implemented server modules: `compute-market.ts` (transactional reservation/delivery), `solana-payment.ts` (exact transaction and signature verification), `compute-payment-rpc.ts` (configured-network RPC boundary), `compute-payment-recovery.ts` (retry/reconciliation), and `compute-market-api.ts` (wallet-scoped handlers and configuration). Migration 0012 adds only two empty payment tables; existing game data is retained.

Authenticated POST actions under `/api/noobius/`: `compute-listing-create`, `compute-listing-cancel`, `compute-payment-quote`, `compute-payment-submit`, `compute-payment-cancel`, and `compute-payment-status`. They use the existing session, same-origin, expected-wallet and request-size checks, plus wallet rate limits. GET `compute-market` returns availability, bounded listings, the connected buyer's pending receipts and five recent purchase receipts. A wallet query guard rejects sessions switched in another tab. Seller cancellation stays accessible even if token configuration is removed. Signing keys, RPC credentials and complete authorized transactions are not returned. New listings/quotes require trading qualification and `NOOBIUS_PAYMENTS_ENABLED=true`; cancellations and pending recovery remain available during a trading pause.

The separate `noobius-payment-recovery` Worker runs every minute and processes at most ten due payments, rotating failures rather than starving later records. Provision the same exact network/mint/precision, RPC configuration and `NOOBIUS_PAYMENT_KEYS` secret on the game and recovery services. Keys are a JSON map of public signer addresses to base64 64-byte keypairs; `NOOBIUS_PAYMENT_SIGNER` chooses the active key for new quotes. Old signer entries must remain until pending payments settle. **Do not change network/mint/decimals with pending payments:** cross-asset policy recovery is not implemented; the service deliberately retains those reservations rather than guessing a replacement asset.

The normal production and staging deploy scripts currently deploy paused trading. They now query the target D1 first and refuse to deploy that paused configuration when any checkout is quoted, recorded or submitted. This prevents a routine deploy from removing the mint/RPC settings needed by recovery after a devnet or future real-token trial. It is a deployment guard, not a substitute for monitoring the pending ledger or preserving signer keys during an intentional payment-enabled release.

Recorded buyer-only payments are released after a trusted configured RPC proves finalized block height beyond the stored lifetime and invalid blockhash. Submitted payments are **not** released merely because `getTransaction` or historical status returns null: Solana's `minimumLedgerSlot` only shows the node's local ledger floor, not that its transaction-history index is complete. A submitted payment with unavailable history stays reserved until its exact finalized transaction can be verified or a separately reviewed recovery process proves non-execution. This protects sellers from a late discovery of a paid transfer, but it can leave a stale reservation requiring operator review; production launch needs a tested archival-RPC and support procedure. Successful chain execution and SQL delivery are not a single distributed transaction: the durable signature and idempotent recovery bridge that gap.

Automated tests exercise competing buyers, concurrent retries, insufficient Compute, altered signatures/messages, wrong network/mint, cancellation, unsigned and buyer-only expiration, submitted-payment preservation under missing RPC history, database failures before authorization and during delivery, ambiguous send responses, exact rebroadcast, finalized failure, and recovery after browser departure. These use real SQLite transactions and cryptographic signing with controlled RPC responses. They are not proof of real devnet transfers or browser-wallet compatibility. Actual test-token transfers, human wallet/mobile checks and a restore-to-chain reconciliation drill remain required before enabling real payments.

## Player checkout checkpoint

The Shop now includes a Compute exchange panel with player-priced sell offers, a purchase review showing exact Compute/token amounts and network, explicit wallet approval, pending status, recent receipts and Solana explorer links. Devnet is labeled as valueless test tokens. Guests cannot sell their practice balance. Disabled trading shows a coming-soon state while retaining cancellation/recovery controls.

`solana-transaction-wallet.ts` implements Wallet Standard sign-only approval, validates account/network before and after the prompt, requires the exact unchanged transaction and checks the buyer signature. It never falls back to sign-and-send. `compute-trading-client.ts` retains the same signed approval for interrupted-submission retries; resuming an existing receipt never opens a signing prompt. Unsigned buyer cancellation competes atomically with durable submission: once recorded, the reservation cannot be cancelled.

407 automated tests plus three tooling checks passed. The new coverage includes cancellation ownership, cancellation before/after durable recording, seller recovery after configuration removal, integer token pricing, changed wallets, unsupported signing features, tampered wallet output, concurrent approval and uncertain submission retries. Desktop and 390px browser checks exercised review, processing, success, failure and paused exchange states using an explicitly labeled local mock adapter. Those temporary preview routes were removed before building; this is rendering evidence, not real-wallet or blockchain evidence.

`node scripts/smoke-solana-devnet.mjs` uses isolated generated test keys and refuses any non-devnet genesis. On September 24, after the generated payer received free devnet SOL, it created a valueless test mint and completed two finalized devnet token purchases through the production settlement modules and API action handlers. Each transferred one test token and delivered 250 Compute once in a local SQLite ledger; duplicate delivery was prevented. The signatures and limits are recorded in [the devnet payment result](verification/2026-09-24-devnet-payment.json). Generated keys and the full proof remain ignored under `.wrangler/`. This does not yet prove hosted D1 settlement or browser-wallet approval.

`npm run smoke:solana-local` installs the Solana Foundation's Surfpool SDK into ignored `.wrangler/surfpool` and runs the same signed purchase on a local devnet fork. The [September 23 result](verification/2026-09-23-local-solana-payment.json) created a valueless six-decimal mint, transferred exactly one test token from buyer to seller, and delivered 250 Compute exactly once through the production quote, co-sign, broadcast and reconciliation modules. The test adapter redirects the HTTPS RPC boundary to the local fork and accommodates Surfpool's `minContextSlot` behavior; it does not validate production RPC finality, the hosted D1 payment routes, or a browser wallet. The public devnet faucet still returned a 429 limit on September 23, so a real devnet transfer remains open. Real-token trading stays disabled.

`npm run smoke:solana-local:token2022` repeats that local-fork proof with a valueless Token-2022 mint, then makes a second purchase through the production marketplace action handlers (list, quote, submit, status). The [September 23 result](verification/2026-09-23-local-solana-token2022.json) transferred one test token and credited 250 Compute exactly once in each path; focused RPC tests rejected fee/hook extensions. A separate run also passed the handler path with the original SPL Token Program. These are in-process action calls against local SQLite, not hosted HTTP authentication, Cloudflare D1 or a browser wallet. To use a different mint later, pause new trades, let all reservations settle or expire, then change the mint, program and decimals together. Never repurpose an in-flight quote for a new asset.

The isolated [Cloudflare staging game](https://noobius-game-staging.rinkydooonso.workers.dev) and staging payment-recovery Worker run against `noobius-game-staging` D1. Hosted login, persistence, assets and paused marketplace smoke checks pass. On September 24, test-token trading was temporarily enabled there, but the hosted purchase stopped before listing creation: Solana's shared public devnet RPC returned HTTP 403 to the Cloudflare Worker. Cloudflare also rejected the Fetch `redirect: 'error'` option; the server RPC and holding checks now use `redirect: 'manual'` and still reject non-2xx responses. No hosted listing or payment was created. Staging trading was paused again, and production was unchanged.

To retry hosted checkout, provide a private HTTPS devnet RPC endpoint through `NOOBIUS_DEVNET_RPC_URL` (and optionally `NOOBIUS_DEVNET_RPC_FALLBACK_URL`) when running `NOOBIUS_STAGE_DEVNET_PAYMENTS=1 npm run deploy:staging:devnet-payments`. The script rechecks the devnet genesis, both finalized test signatures, mint compatibility and empty pending-payment ledger. It supplies RPC URLs and the generated authorization key through a temporary Cloudflare secrets file, then removes that file. It deploys the recovery Worker before the game Worker and never targets production or mainnet. A passing deployment alone is not payment acceptance.

`npm run smoke:staging:token-checkout` has been run but has **not passed**. It authenticates the two generated devnet test accounts to staging and seeds only their staging profiles with a documented trading fixture, then currently stops at the shared-RPC 403 before a listing is made. With a working private devnet RPC, it will create a listing and quote through hosted HTTP, sign with the generated buyer key, submit the payment, wait for finality, and check exact token balance changes plus once-only Compute delivery in staging D1. The fixture does not prove that the buyer earned Compute through gameplay; that path has separate hosted gameplay tests. Browser-extension approval and payment recovery after a browser exit remain later checks.

A [staging D1 export/import drill](verification/2026-09-23-staging-restore-drill.json) restored a copy into a separate Cloudflare database and matched selected player, session, migration and empty marketplace counts. The clone and export were deleted afterward. This verifies a basic restore path, not a production restore or reconciliation of payments made after a backup; token trading remains off.
